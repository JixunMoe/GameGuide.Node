var config = {
  // DuoShuo short name.
  // Set 'false' to disable comment.
  ds_short_name: 'example',

  // Allow user to register or not.
  allow_reg: false,

  // Google Analytics ID
  // `false` to disable.
  google_analytics: false
};

module.exports = config;